# CNN CIFAR-10 Image Classification

An end-to-end Deep Learning project focused on building and training a Convolutional Neural Network (CNN) to classify images into 10 distinct classes using the classic CIFAR-10 dataset. The pipeline covers data preprocessing, data augmentation, model architecture design, and final evaluation.
